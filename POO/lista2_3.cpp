//Atributos são as propriedades de um objeto. Métodos são as ações que um objeto pode realizar. Naturalmente, os atributos de um objeto podem mudar de acordo com o tempo.
//De fato, a execução de um método muda os valores dos atributos do objeto responsável pela execução.

//Exemplo:

 _________________________
|        Pessoa           |
|-------------------------|
| - nome: string          |
| - idade: int            |
|-------------------------|
| + setNome(novoNome: string) |
| + setIdade(novaIdade: int) |
| + aniversario()         |
| + getInfo()             |
|_________________________|
